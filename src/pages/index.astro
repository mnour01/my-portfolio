---
const sections = [
  { label: "Expériences", link: "/my-portfolio/experiences" },
  { label: "Formations", link: "/my-portfolio/formations" },
  { label: "Compétences", link: "/my-portfolio/competences" },
  { label: "Langues", link: "/my-portfolio/langues" },
  { label: "Centres d’intérêt", link: "/my-portfolio/interets" },
];
---

<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <title>Mehdi Nour – Data Scientist</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500&family=Inter&display=swap" rel="stylesheet" />
    <style>
      :root {
        --bg: #f0f4f8;
        --accent: #0a806f;
        --text: #1a1a1a;
        --hover: #17c8a3;
        --card: #ffffff;
        --card-shadow: rgba(0, 0, 0, 0.05);
      }

      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      body {
        font-family: 'Inter', sans-serif;
        background: var(--bg);
        color: var(--text);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
      }

      header {
        text-align: center;
        padding: 4rem 2rem 2rem;
      }

      .photo img {
        width: 160px;
        height: 160px;
        border-radius: 50%;
        border: 4px solid var(--accent);
        object-fit: cover;
        box-shadow: 0 0 16px var(--card-shadow);
      }

      h1 {
        font-family: 'Orbitron', sans-serif;
        font-size: 2rem;
        margin-top: 1.2rem;
        color: var(--accent);
      }

      p {
        font-size: 1.1rem;
        max-width: 650px;
        margin: 1rem auto 2rem;
        text-align: center;
      }

      .nav-grid {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 1rem;
        padding: 2rem;
      }

      .nav-grid a {
        background: var(--card);
        padding: 0.9rem 1.8rem;
        border-radius: 8px;
        text-decoration: none;
        color: var(--accent);
        font-weight: 600;
        border: 1px solid var(--accent);
        transition: all 0.3s ease;
        box-shadow: 0 2px 6px var(--card-shadow);
      }

      .nav-grid a:hover {
        background: var(--accent);
        color: white;
      }

      footer {
        margin-top: auto;
        text-align: center;
        padding: 2rem;
        font-size: 0.9rem;
        color: #555;
      }

      @media (max-width: 768px) {
        .nav-grid {
          flex-direction: column;
          align-items: center;
        }
      }
    </style>
  </head>

  <body>
    <header>
      <div class="photo">
        <img src="images/pdp_mehdi.png" alt="Photo de Mehdi Nour" />
      </div>
      <h1>Mehdi Nour</h1>
      <p>
        Data Scientist spécialisé en intelligence artificielle, traitement de données, visualisation, cloud et transition énergétique.
      </p>
    </header>

    <nav class="nav-grid">
      {sections.map(({ label, link }) => (
        <a href={link}>{label}</a>
      ))}
    </nav>

    <footer>
      &copy; {new Date().getFullYear()} Mehdi Nour • nour.mehdi@yahoo.com
    </footer>
  </body>
</html>