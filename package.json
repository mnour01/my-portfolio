{
  "name": "my-portfolio",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
  "dev": "astro dev",
  "build": "astro build",
  "preview": "astro preview",
  "deploy": "astro build && gh-pages -d dist"
},
  "dependencies": {
    "astro": "^5.7.3"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
